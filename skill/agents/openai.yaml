interface:
  display_name: "Codex CLI Delegate"
  short_description: "Delegate through MCP, resume sessions, and verify results"
  default_prompt: "Use $codex-cli-delegate through MCP to delegate an authorized task and independently verify its result."
policy:
  allow_implicit_invocation: true
