interface:
  display_name: "Codex CLI Delegate"
  short_description: "Delegate CLI work, verify completion, and review original evidence"
  default_prompt: "Use $codex-cli-delegate to delegate an authorized task and independently verify its result."
policy:
  allow_implicit_invocation: true
