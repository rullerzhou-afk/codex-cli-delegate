interface:
  display_name: "Codex CLI Delegate"
  short_description: "Delegate through MCP, resume sessions, and verify results"
  default_prompt: "Use $codex-cli-delegate through MCP for every explicitly named external backend, preserve established user aliases, never substitute a native worker for that backend, and independently verify each result."
policy:
  allow_implicit_invocation: true
